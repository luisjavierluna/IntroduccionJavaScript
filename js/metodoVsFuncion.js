const numero1 = 20
const numero2 = "20"

console.log(parseInt(numero2)) // funcion, primero se pone el nombre y después dentro de parentesis la variable que cambiará
console.log(numero1.toString()) // método, primero se pone la variable que cambiará y despues con un punto el método